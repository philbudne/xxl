// tokenize this file!!!
var token = System.tokenizer("test/008-token.xxl", '=<>!+-*&|/%^', '=<>&|'); // returns generator

while (true) {
    var t = token();
    if (!t) {
	leave_while();
    }
    System.print(t.where, t.type, t.value);
}
