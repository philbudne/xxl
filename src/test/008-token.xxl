// tokenize this file!!!
var token = __xxl.tokenizer("test/008-token.xxl", '=<>!+-*&|/%^', '=<>&|'); // returns generator

leave_while:
while (true) {
    var t = token();
    if (!t) {
	leave_while();
    }
    __xxl.print(t.where, t.type, t.value);
}
